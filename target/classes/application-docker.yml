spring:
  datasource:
    url: jdbc:postgresql://boom-postgres:5432/boom
    username: boom
    password: boom

  flyway:
    enabled: true
    locations: classpath:db/migration

  data:
    redis:
      host: boom-redis
      port: 6379

logging:
  level:
    root: INFO
    com.boom: DEBUG
